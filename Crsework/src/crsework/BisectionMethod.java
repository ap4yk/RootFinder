/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package crsework;

/**
 *
 * @author av7521l
 */
public class BisectionMethod {

    public BisectionMethod() {
    }
    
    public String findRoots(double range, double xInput, double decimal) {
        return null;
       /* final double diff = decimal;
        String allIntervals="";
             double xlower, xupper, xnew, y_m, y_a;
             int iteration = 0;
             xlower = 0;  xupper = 5;
             while ( (xupper-xlower) > diff )
             {
                 iteration = iteration +1;
                xnew = (xlower+xupper)/2;           // Mid point
                y_m = xnew*xnew - 5.0;       // y_m = f(xnew)
                y_a = xlower*xlower - 5.0;       // y_a = f(xlower)
                if ( (y_m > 0 && y_a < 0) || (y_m < 0 && y_a > 0) )
                {  // f(a) and f(m) have different signs: move b
                   xupper = xnew;
                }
                else
                {  // f(xlower) and f(xnew) have same signs: move xnew
                   xlower = xnew;
                }
                allIntervals=allIntervals+"New interval " + iteration + ": [" + xlower + " .. " + xupper + "]\n";
                System.out.println("New interval " + iteration + ": [" + xlower + " .. " + xupper + "]");   
             }
             allIntervals=allIntervals+"Approximate solution after iteration " + iteration +  " = " + (xlower+xupper)/2+"\n";
             System.out.println("Approximate solution after iteration " + iteration +  " = " + (xlower+xupper)/2 );
        return allIntervals;*/
    }
    
}
